// check the route on the url
// if the route is home, then load the home.html file
// if the route is about, then load the about.html file

let routes = {
  home: function() {
    output.innerHTML = '<h1>Home</h1>';
  }
}